{
  "name": "autolocaltimerolebot",
  "version": "1.0.0",
  "description": "A discord bot that changes the role name to local time of a user",
  "main": "main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Xubinex",
  "license": "ISC",
  "dependencies": {
    "discord.js": "^13.3.1",
    "dotenv": "^10.0.0"
  }
}
